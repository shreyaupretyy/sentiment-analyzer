# App settings
APP_NAME=sentiment-analyzer
APP_VERSION=0.1.0
ENVIRONMENT=development

# Server settings
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# Optional Redis cache (not used in basic implementation)
REDIS_HOST=redis
REDIS_PORT=6379